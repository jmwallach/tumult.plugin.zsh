#!/usr/bin/env bash
#
# Throttle Time Machine so the backup doesn't take so long
#
# Copyright 2021, Joe Block <jpb@unixorn.ne>

source tumult-functions

only-run-on Darwin

exec sudo sysctl debug.lowpri_throttle_enabled=1
